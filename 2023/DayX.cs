
public static class DayX
{
    public static void Part1()
    {
        using FileStream fs = File.OpenRead(@"DayX.txt");
        using TextReader input = new StreamReader(fs);

        string line = input.ReadLine();
        while (line != null)
        {
        }
    }

    public static void Part2()
    {
        using FileStream fs = File.OpenRead(@"DayX.txt");
        using TextReader input = new StreamReader(fs);

        string line = input.ReadLine();
        while (line != null)
        {
        }
    }
}